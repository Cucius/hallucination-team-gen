const Employee = require("../lib/employee");
const Engineer = require("../lib/engineer");
const Intern = require("../lib/intern");
const Manager = require("../lib/manager");

describe("Employee", () => {
  it("should prompt the user for input and create an object with the name, id and email", () => {});

  describe("Manager", () => {
    it("should inherit name id and email and prompt input for office number", () => {});
  });

  describe("Engineer", () => {
    it("should inherit name id and email and prompt input for GitHub username", () => {});
  });

  describe("Intern", () => {
    it("should inherit name id and email and prompt input for school", () => {});
  });
});
